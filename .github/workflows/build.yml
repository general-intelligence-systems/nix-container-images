name: Build and Push Container Image

on:
  workflow_dispatch:
    inputs:
      image:
        description: "Image name (matches filename in images/ without .nix)"
        required: true
        type: string
  workflow_call:
    inputs:
      image:
        description: "Image name (matches filename in images/ without .nix)"
        required: true
        type: string

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/general-intelligence-systems/runs-on-github:latest
    steps:
      - uses: actions/checkout@v4

      - name: Build image
        run: |
          export HOME=/root
          git config --global --add safe.directory '*'
          rm -rf /homeless-shelter
          env -u LD_LIBRARY_PATH nix build .#${{ inputs.image }}

      - name: Load image into Docker
        run: docker load < result

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image
        run: docker push ghcr.io/general-intelligence-systems/${{ inputs.image }}:latest
