name: Test / Nix

on:
  workflow_dispatch:
  workflow_call:

jobs:
  nix-build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/general-intelligence-systems/base-runner:latest
    steps:
      - uses: actions/checkout@v4

      - name: debug
        run: |
          whoami
          echo $PATH
          
      - name: Check nix flake
        run: nix flake check
