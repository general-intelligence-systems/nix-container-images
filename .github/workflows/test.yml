name: Test / Nix

on:
  workflow_dispatch:
  workflow_call:

jobs:
  nix-build:
    runs-on: ghcr.io/general-intelligence-systems/runs-on-nix:latest
    steps:
      - uses: actions/checkout@v4

      - name: Check nix flake
        run: nix flake check
